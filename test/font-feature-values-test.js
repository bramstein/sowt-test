describe('@font-feature-values', function () {
  it('supports the syntax', function () {
    var style = document.body.style;

    expect(window.CSSFontFeatureValuesRule, 'to be an object');
  });
});
